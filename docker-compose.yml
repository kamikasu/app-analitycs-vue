# # For more information: https://laravel.com/docs/sail
# version: '3'
# services:
#     laravel.test:
#         build:
#             context: ./docker/8.1
#             dockerfile: Dockerfile
#             args:
#                 WWWGROUP: '${WWWGROUP}'
#         image: sail-8.1/app
#         extra_hosts:
#             - 'host.docker.internal:host-gateway'
#         ports:
#             - '${APP_PORT:-80}:80'
#         environment:
#             WWWUSER: '${WWWUSER}'
#             LARAVEL_SAIL: 1
#             XDEBUG_MODE: '${SAIL_XDEBUG_MODE:-off}'
#             XDEBUG_CONFIG: '${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}'
#         volumes:
#             - '.:/var/www/html'
        # networks:
        #     - sail
        # depends_on:
            # - mysql
    # mysql:
    #     image: 'mysql/mysql-server:8.0'
    #     ports:
    #         - '${FORWARD_DB_PORT:-3306}:3306'
    #     environment:
    #         MYSQL_ROOT_PASSWORD: '${DB_PASSWORD}'
    #         MYSQL_ROOT_HOST: "%"
    #         MYSQL_DATABASE: '${DB_DATABASE}'
    #         MYSQL_USER: '${DB_USERNAME}'
    #         MYSQL_PASSWORD: '${DB_PASSWORD}'
    #         MYSQL_ALLOW_EMPTY_PASSWORD: 1
    #     volumes:
    #         - 'sail-mysql:/var/lib/mysql'
    #     networks:
    #         - sail
    #     healthcheck:
    #         test: ["CMD", "mysqladmin", "ping", "-p${DB_PASSWORD}"]
    #         retries: 3
    #         timeout: 5s
# networks:
#     sail:
#         driver: bridge
# volumes:
#     sail-mysql:
#         driver: local
version: '3.8'
services:

  app-dashboard:
    build: .
    environment:
      - ENV=LOCAL
      - CONTAINER_ROLE=app
    ports:
      - 80:80
    volumes:
      - ./:/var/www/html/
  #   # command: bash -c "mkdir -p /constantes && cp -R constantes /constantes"
  # queue:
  #   build: .
  #   depends_on:
  #     - docker-clireport.com
  #   volumes:
  #     - .:/var/www/html
  #   environment:
  #     APP_ENV: local
  #     CONTAINER_ROLE: queue
#   db_mongo:
#     image: mongo:5.0.0
#     ports:
#       - 27017:27017
#   db_maria:
#     image: mariadb:10.5.13
#     environment:
#       MYSQL_ROOT_PASSWORD: root
#     ports:
#       - 3306:3306
#   db_redis:
#     image: redis:latest
#     ports:
#       - 6379:6379

# networks:
#   default:
#     name: lab_proxy
